# Image Compare Annotator 使用说明

## 1. 工具用途

这是一个本地网页图片对比标注工具，用于：
- 加载 1 组原图目录
- 加载 1 组或多组效果图目录
- 按同名文件主干匹配图片
- 通过滑块或快捷键快速对比原图与效果图
- 进行合格 / 不合格标注
- 导出标注结果与汇总信息

---

## 2. 启动方式

### Ubuntu / Linux

```bash
./start.sh
```

### macOS

优先使用：
- 双击 `ImageCompareAnnotator.app`

备选方式：
- 双击 `start.command`

### Windows

- 双击 `start.bat`

---

## 3. 启动后的访问

程序启动后会自动打开浏览器。
如果没有自动打开，请手动访问终端输出中的地址，例如：

```text
http://127.0.0.1:8765
```

如果默认端口被占用，程序会自动切换到下一个可用端口。

---

## 4. 数据加载方式

### 模式 A：服务端目录模式
适合数据已经在服务所在机器上的场景。

需要填写：
- 原图目录
- 一个或多个效果图目录

### 模式 B：浏览器本地模式
适合图片在当前电脑本地、但不想上传原图到服务器的场景。

需要选择：
- 原图文件夹
- 一个或多个效果图文件夹

可选：
- 导入之前导出的结果 JSON，用于恢复进度

---

## 5. 标注规则

- `=`：标记为合格
- `-`：标记为不合格
- `待定` 视为未标注

多列效果图场景下：
- 当一列被标注后，切换到下一组时
- 其余未标列会根据竞争关系自动补全相反结果

---

## 6. 对比方式

工具栏提供三种对比模式，可随时切换（快捷键 `S` 循环）：

- **滑块叠加**：原图与效果图叠加，拖动中间的竖向手柄左右擦除对比；按 `空格` 可在「全原图 ↔ 全效果图」之间快速翻转。
- **并排对比**：原图与效果图左右并排显示。
- **像素差异**：在浏览器端逐像素计算原图与效果图的差异，差异区域以红色高亮，其余区域显示为灰度底图；工具栏「灵敏度」滑块可调阈值，右上角实时显示差异占比。

三种模式都支持：

- **滚轮缩放**（最高 8 倍），**按住拖拽平移**，双击或按 `F` 重置视图，方便像素级细节核对。
- **放大镜**（快捷键 `L`，或工具栏放大镜按钮）：鼠标悬停处显示局部放大的圆形镜片（差异模式下不可用）。

## 撤销

任意标注 / 自动补全后，可用 `U`（或 `Ctrl/⌘ + Z`、工具栏撤销按钮）逐步回退，最多保留最近 50 步。

## 7. 分组导航

左侧「分组导航」面板可：

- 按文件名搜索分组
- 按 `全部 / 未完成 / 已完成` 过滤
- 点击任意分组直接跳转
- 工具栏「下一个未完成」按钮（快捷键 `N`）可一键跳到下一个尚未标注完的分组

## 8. 快捷键

- `=`：标合格
- `-`：标不合格
- `← / ↑`：上一组
- `→ / ↓`：下一组
- `N`：跳到下一个未完成分组
- `U` / `Ctrl·⌘ + Z`：撤销上一步
- `1 / 2 / 3 …`：切换到对应列效果图
- `Tab`：逐列切换效果图
- `空格`：滑块模式下在最左 / 最右之间快速翻转
- `S`：循环 滑块 / 并排 / 差异 对比模式
- `L`：放大镜开关
- `F`：重置缩放与平移
- `?`：打开快捷键帮助

---

## 9. 导出与分拣

支持导出：
- 合格文件名列表（.txt）
- 不合格文件名列表（.txt）
- 标注汇总 JSON
- 标注明细 CSV 表格（含 BOM，Excel 可直接打开）

**按结果分拣文件**（仅服务端目录模式）：一键把已标注的效果图复制到 `数据集_sorted/qualified` 与 `数据集_sorted/unqualified` 子目录，文件名为 `组名__col列号.扩展名`，复制不影响原始数据。

---

## 10. 断点续标

- 服务端目录模式：优先使用服务端已有记录
- 浏览器本地模式：可导入之前导出的 JSON 恢复进度
- 标注结果会实时保存

---

## 11. 常见问题

### Q1：浏览器没有自动打开怎么办？
手动访问终端里打印的本地地址。

### Q2：端口被占用了怎么办？
程序会自动切换到下一个可用端口。

### Q3：macOS 双击没有反应怎么办？
优先双击 `ImageCompareAnnotator.app`。如果系统拦截，请右键选择“打开”。

### Q4：把服务部署在服务器、本地浏览器访问，安全吗？
服务默认监听 `0.0.0.0`，方便从其他机器访问。为避免任意文件被读取，服务端图片接口 `/api/file` **只允许读取已加载数据集目录（原图目录 + 效果图目录）内的图片文件**，目录之外的路径、非图片文件、以及 `..` 越权路径都会被拒绝（返回 403）。如果你发现某张图返回 403，请确认它确实位于已加载的目录内。

> 注意：该接口本身不带账号鉴权，请仅在可信网络内部署；如需对外暴露，建议在前面加一层反向代理做访问控制。
